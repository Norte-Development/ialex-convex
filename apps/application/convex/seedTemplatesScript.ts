/**
 * Script to seed sample templates into the database.
 * 
 * This is a one-time script to populate the database with common legal templates.
 * 
 * To run this script:
 * 1. Make sure your Convex dev server is running
 * 2. Run: npx convex run functions:seedTemplates
 * 
 * The script will create 7 sample templates covering common legal documents:
 * - Demanda Civil (HTML)
 * - Contestación de Demanda (HTML)
 * - Recurso de Apelación (JSON)
 * - Demanda de Divorcio (HTML)
 * - Carta Documento (HTML)
 * - Contrato de Compraventa (HTML)
 * - Alegatos (JSON)
 * 
 * All templates are created as public system templates, meaning they are
 * accessible to all users and marked as created by "system".
 */

// This file is for documentation purposes only.
// To run the seed, use: npx convex run functions:seedTemplates

