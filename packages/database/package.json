{
  "name": "@ialex/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs --sourcemap --out-dir dist --tsconfig tsconfig.json"
  },
  "dependencies": {
    "@qdrant/js-client-rest": "^1.15.1"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "~5.9.3"
  }
}
